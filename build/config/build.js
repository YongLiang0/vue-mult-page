/**
 *  **注=========================================================================
 * 当单页面应用页面数量比较大的时候。本地运行dev的时候，热更新加载会变得卡顿，
 * 所以，当页面数量比较多的时候，开发页面，可以打开下面isUse为true，然后写入你正在开发的页面的名称，这样
 * 就只会单独运行buildList中的页面，加快热更新的速度
 * 
 *  **注=========================================================================
 * 在项目打包发布，千万注意，isUse必须为false。否则只会打包buildList中的页面。所以这个功能谨慎使用。
 */

module.exports = {
    isUse : false,
    buildList : [
        'index'
    ]
}